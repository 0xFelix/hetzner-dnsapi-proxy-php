{
    "name": "hetzner-dnsapi-proxy-php",
    "image": "mcr.microsoft.com/devcontainers/php:8",
    "customizations": {
        "vscode": {
            "extensions": [
                "bmewburn.vscode-intelephense-client",
                "recca0120.vscode-phpunit"
            ]
        }
    },
    "forwardPorts": [8080],
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libicu-dev && sudo PHP_INI_DIR=/usr/local/etc/php docker-php-ext-install intl && composer install"
}
